# Primary deps; install with: pip install -r requirements.txt
# For CPU-only (recommended for local/Mac): pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
torchvision>=0.17
pillow>=10.0
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
matplotlib>=3.8
requests>=2.31
tqdm>=4.66
